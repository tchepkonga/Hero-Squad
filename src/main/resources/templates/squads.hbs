{{#partial "content"}}
    {{#if squads}}
        {{#each squads}}
            <h5>Squad name is: <i>{{name}}</i></h5>
            <p>Cause is: {{cause}}</p>
            <button class="btn btn-info"><a href="/hero-form">Add a Hero to {{name}}</a></button>
            {{squadHeroes}}
        {{/each}}
    {{else}}
        No Squads created yet!!
    {{/if}}
    <h4><a href="/squads-form">Add a new Squad</a></h4>
{{/partial}}
{{> layout.hbs}}